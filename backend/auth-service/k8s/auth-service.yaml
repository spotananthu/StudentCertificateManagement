apiVersion: v1
kind: Service
metadata:
  name: auth-service
  labels:
    app: auth-service
spec:
  type: NodePort   # Easy to test with Minikube
  selector:
    app: auth-service
  ports:
    - name: http
      port: 8081        # Service port inside cluster
      targetPort: 8081  # Container port
      nodePort: 30081   # External port on Minikube node (30000–32767)
