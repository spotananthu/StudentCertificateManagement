apiVersion: apps/v1
kind: Deployment
metadata:
  name: university-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: university-postgres
  template:
    metadata:
      labels:
        app: university-postgres
    spec:
      containers:
        - name: university-postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          envFrom:
            - configMapRef:
                name: university-postgres-config
            - secretRef:
                name: university-postgres-secret
          volumeMounts:
            - name: university-postgres-storage
              mountPath: /var/lib/postgresql/data
              subPath: postgres
      volumes:
        - name: university-postgres-storage
          persistentVolumeClaim:
            claimName: university-postgres-pvc
